# Log Contract

- `logs/interaction.md` records manual interaction when it happens.
- Unattended runs may leave `logs/interaction.md` with a minimal no-interaction record.
- `logs/trace/` stores execution traces and summaries.
- Trace outputs should be sufficient to confirm selected source paths, README detection, and verification state.

# Result Contract

- `result/output.md` is the evaluator-facing primary output.
- `result/issues/00-summary.md` is the evaluator-facing issue summary.
- `SOURCE_ROOT/.loopforge/` contains internal execution evidence only.
- Internal evidence must not replace `result/` and `logs/` as the evaluator entrypoint.

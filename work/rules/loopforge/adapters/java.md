# Java Adapter

- align with existing build layout such as Maven or Gradle if present
- treat API contracts and package boundaries as first-class evidence in consistency checks
- do not guess alternate test entrypoints beyond configured verification

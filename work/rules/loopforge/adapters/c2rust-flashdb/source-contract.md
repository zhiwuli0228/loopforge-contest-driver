# C2Rust FlashDB Source Contract

- `SOURCE_ROOT` is read-only.
- Requirements must be read from README candidates under `SOURCE_ROOT`.
- Accept either `SOURCE_ROOT/src` + `SOURCE_ROOT/tests` or `SOURCE_ROOT/FlashDB/src` + `SOURCE_ROOT/FlashDB/tests`.
- Local development fallback may resolve to `.code/FlashDB`.
- `work/code/README.md` is a contest requirement document, not a valid source root.
- Do not write reports, snapshots, runtime state, or generated files under `SOURCE_ROOT`.

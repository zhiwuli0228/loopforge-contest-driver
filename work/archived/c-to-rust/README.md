# Archived C-to-Rust Assets

This directory preserves the repository's historical C2Rust migration assets.

These files are retained for audit and historical recovery only. They are not
part of the authoritative `consistency-check` workflow, and the default
runtime, profiles, stages, guards, and skills must not resolve to them.

See `manifest.json` for the original paths and migration reasons.

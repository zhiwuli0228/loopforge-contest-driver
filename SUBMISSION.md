# Submission

This repository is submitted as a LoopForge execution adapter for the FlashDB C-to-Rust contest workflow.

Primary evaluator-facing outputs:

- `result/output.md`
- `result/issues/00-summary.md`
- `logs/trace/`

Execution metadata and adapter state are also recorded under `SOURCE_ROOT/.loopforge/`.

## 1. 分析契约与测试基线

- [x] 1.1 定义从当前输入解析核心目录和预处理配置的通用契约，并增加只读输入 fixture
- [x] 1.2 定义规范化分析模型以及八类证据文件的 schema version、稳定 ID、source evidence 和统一 run metadata
- [x] 1.3 为复杂 `typedef`、结构体/枚举、函数指针、跨文件声明定义、条件编译和源测试发现添加失败优先的分析测试
- [x] 1.4 添加输入树文件集合、内容摘要和权限在成功/阻塞分析前后保持不变的测试

## 2. 完整源码分析器

- [x] 2.1 实现编译数据库读取或受控编译参数构造，并记录解析器版本、目标、宏、include 路径和翻译单元
- [x] 2.2 接入 C 预处理/AST 前端，解析核心文件、宏变体、声明、定义、`typedef`、结构体、联合体、枚举和函数指针
- [x] 2.3 构建规范化符号表及声明/定义、include、调用、类型依赖和全局状态读写关系
- [x] 2.4 对无法解析的核心翻译单元、函数和类型生成带源码位置的确定阻塞原因，禁止文本回退将其标记为覆盖
- [x] 2.5 生成并 schema 校验 `source-inventory.json`、`public-api-map.json`、`type-map.json`、`call-graph.json`、`global-state-map.json` 和 `preprocessor-variants.json`
- [x] 2.6 添加相同输入和配置重复运行时规范化分析内容稳定一致的测试

## 3. 独立扫描与交叉验证

- [x] 3.1 实现不导入主分析抽取逻辑的独立文件/词法扫描器，输出非空核心源文件、公开 API 候选和源测试集合
- [x] 3.2 生成并 schema 校验 `independent-source-scan.json`，为每个候选项保留来源位置
- [x] 3.3 实现双方集合比较和带规则标识、原因、源码证据的差异分类，拒绝无证据白名单
- [x] 3.4 添加主分析遗漏 API、文件或测试时交叉验证必然失败的负向测试

## 4. Fail-closed 分析门禁

- [x] 4.1 实现三个基础计数严格大于零以及核心文件、公开 API 和核心类型成员 100% 覆盖检查，确保缺失项不从分母排除
- [x] 4.2 实现悬空符号、缺少公开 API 声明/定义/证据、核心解析失败和未解释集合差异的阻塞检查
- [x] 4.3 生成 `analysis-verification.json`，包含每项门禁输入、结论、全部阻塞原因、首个阻塞点和总体状态
- [x] 4.4 拒绝 schema 无效、路径不存在、run ID 不一致或混合旧运行产物的证据包，并添加对应负向测试
- [x] 4.5 将门禁接入 runner，只有分析验证通过才允许进入语义建模/迁移规划，失败统一输出 `BLOCKED_WITH_REPORT`

## 5. 真实项目验证与文档

- [x] 5.1 在与项目身份无关的 C fixture 上执行集成测试，证明核心源文件、公开 API、核心类型与测试满足非真空和 100% 验收条件
- [x] 5.2 注入核心文件解析失败、缺失结构体字段、悬空 API 和扫描集合不一致，验证每类问题均阻止代码生成
- [x] 5.3 验证八类证据的交叉引用、统一运行标识、确定性和输入只读性，并保存测试日志
- [x] 5.4 更新运行时/适配器文档，说明解析依赖、核心范围配置、证据字段和阻塞诊断方法

## 6. 去除题目定制

- [x] 6.1 删除固定仓库、提交、宏组合、目录排除和精确指标验收
- [x] 6.2 将范围和预处理策略改为从当前输入布局及编译数据库派生
- [x] 6.3 将运行时与 schema 改为通用 C 源码分析命名，并验证下游兼容性

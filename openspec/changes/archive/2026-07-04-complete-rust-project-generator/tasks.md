## 1. 生成证据契约与测试基线

- [x] 1.1 固定 Change 1/2 输入文件、schema 兼容范围、分析/规划 run ID、输入摘要和父子证据链一致性规则，并实现无效、陈旧及混合输入的负向 fixture
- [x] 1.2 定义生成运行元数据、稳定实现 ID、工程内容摘要以及 `implementation-map.json`、`unsupported-functions.json`、`module-edge-map.json` 和生成验证报告的 schema 与交叉引用规则
- [x] 1.3 固定 Rust edition、最低工具链、依赖版本、`Cargo.lock` 策略和规范化确定性输出规则，并将其加入生成运行身份
- [x] 1.4 建立项目无关的生成 fixture，覆盖公共 API、领域状态、持久化和由迁移计划声明的外部端口，并验证生成路径不含项目名或符号前缀分支

## 2. 完整 Cargo 工程生成

- [x] 2.1 实现已验证证据链加载器，拒绝门禁未通过、必需文件缺失、schema 不兼容、运行身份混合或摘要不一致的输入，且失败时不覆盖现有工程
- [x] 2.2 生成确定性的 Cargo 清单、锁文件、crate 入口和 Rust 模块树，并将每个模块关联到迁移计划中的稳定模块 ID 与职责
- [x] 2.3 生成迁移计划声明的核心 C 类型、字段、枚举、函数指针、状态及所有权/借用/生命周期约束对应的 Rust 类型和状态容器
- [x] 2.4 从边界依赖生成外部端口及适配器边界，保留规划的错误、原子性和线程安全语义，且不硬编码端口领域
- [x] 2.5 按行为契约和状态转换实现可证明的核心逻辑，并为每个实现记录契约、状态效果和源码位置；先执行项目无关的通用识别/修复规则，不可证明的实现才进入不支持门禁而非生成伪实现
- [x] 2.6 对无法生成的核心函数或公开 API 先记录结构化诊断并继续主生成流程，在最终阶段统一执行一次项目无关修复尝试；仍未解决时写入最终报告和不支持项，验证失败路径不会产生 `todo!()`、`unimplemented!()` 或其他占位实现

## 3. 实现与模块关系完整性验证

- [x] 3.1 从上游源码事实独立计算核心函数、公开 API、模块和核心调用边分母，验证函数/API 映射率 100% 且缺失项不会从分母排除
- [x] 3.2 生成 `implementation-map.json`，验证每个映射的 Rust 模块、符号和源码范围实际存在、唯一且具有非空可达实现
- [x] 3.3 生成 `module-edge-map.json`，验证每条核心调用边被直接保留或具有迁移计划批准的合并、拆分或依赖反转等价证据
- [x] 3.4 实现占位与伪实现扫描，检测 `todo!()`、`unimplemented!()`、占位 panic、空函数、非法恒定返回、关键输入未使用和缺失状态效果
- [x] 3.5 实现占位启发式裁决记录，要求合法常量或空操作具有行为契约和源码证据，任何未裁决核心命中均输出 `BLOCKED_WITH_REPORT`
- [x] 3.6 注入缺失函数/API 映射、缺失调用边、空实现和恒定成功伪逻辑，证明每类缺陷都阻止工程生成门禁通过

## 4. 构建、安全与原子发布门禁

- [x] 4.1 固定不可由格式化稀释的 `unsafe` 统计口径和非零分母规则，实现逐处必要性、安全不变量、封装边界及源码位置审计
- [x] 4.2 强制 `unsafe` 比例严格小于 0.10，添加比例等于/超过阈值、零分母和缺失说明的负向测试
- [x] 4.3 在候选工程执行 `cargo build --locked`，记录工具链、命令、工作目录、退出码和完整日志，并验证构建失败或陈旧锁文件均阻塞
- [x] 4.4 在暂存目录完成工程生成、schema、交叉引用、完整性、占位、unsafe 和构建验证，仅在全部通过后原子发布工程与证据
- [x] 4.5 生成统一验证报告，记录每项门禁的分母、结果、证据路径、全部阻塞原因和首个阻塞点，并将失败统一映射为 `BLOCKED_WITH_REPORT`
- [x] 4.6 测试写入中断、schema 失败和构建失败不会留下部分候选结果，且上一版已验证工程和证据保持不变

## 5. 受控增量重新生成

- [x] 5.1 从已验证实现与模块边图计算目标模块及直接依赖的允许变更闭包，记录纳入边证据、禁止变化文件和未知目标阻塞诊断
- [x] 5.2 在运行前保存规范化生成树文件清单和哈希，并将增量计划绑定到父生成 run ID、父工程摘要、目标、输入摘要和工具版本
- [x] 5.3 在独立暂存目录只重新生成允许闭包，对候选工程重新执行完整生成的映射、占位、unsafe 和 `cargo build --locked` 门禁
- [x] 5.4 比较前后文件集合与内容哈希，要求闭包外文件完全不变，并生成 `incremental-regeneration-report.json`
- [x] 5.5 仅在候选门禁和边界核对全部通过时原子发布增量结果；验证越界变化、陈旧父工程和候选失败均回退到原工程
- [x] 5.6 对相同父工程和请求重复运行，验证除运行元数据外候选内容、变化集合、闭包和哈希报告确定性一致

## 6. 流水线接线与真实项目验证

- [x] 6.1 将完整工程生成阶段接入 runner，只有 Change 1/2 输入验证通过才运行，且只有生成验证通过才允许 Change 4 消费工程
- [x] 6.2 在通用 fixture 上验证核心函数与公开 API 映射率 100%、核心调用边完整、`unsupported-functions.json` 为空且无占位或伪实现
- [x] 6.3 验证生成工程包含全部迁移计划模块和规划端口，执行 `cargo build --locked` 返回零，并保存与同一生成运行绑定的原始日志
- [x] 6.4 验证 `unsafe` 比例严格小于 0.10、每处均有必要性说明，并证明统计结果在格式化和重复生成后稳定
- [x] 6.5 对代表性业务模块和边界模块执行单点增量生成，证明目标/直接依赖范围由迁移计划计算且无关模块哈希不变
- [x] 6.6 更新运行时与证据契约文档，说明完整/增量生成接口、工程布局、门禁、阻塞诊断以及 Change 4/5 的消费边界

# Optional Tool Wrapper Rule

## Status

Optional only. Not part of the primary contest path.

## Purpose

If project-local OpenCode tools are supported, a lightweight wrapper may invoke the generated Python runner and return its output.

## Constraint

The wrapper must not replace the skill, rules, or runner as the source of workflow logic.
